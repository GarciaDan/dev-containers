# Hoist Playwright and ESLint binaries to the top-level node_modules/.bin
# so VS Code extensions (Playwright, ESLint) can discover them automatically.
public-hoist-pattern[]=*playwright*
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
